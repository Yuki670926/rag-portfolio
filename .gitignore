# Local .terraform directories
.terraform/

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version
# control as they are data points which are potentially sensitive and subject
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock info files created by terraform apply
.terraform.tfstate.lock.info

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Optional: ignore graph output files generated by `terraform graph`
# *.dot

# Optional: ignore plan files saved before destroying Terraform configuration
# Uncomment the line below if you want to ignore planout files.
# planout
*.zip

# Lambda installed packages
lambda/ingest/bin/
lambda/ingest/dateutil/
lambda/ingest/six.py
lambda/ingest/*.dist-info/
lambda/query/bin/
lambda/query/dateutil/
lambda/query/six.py
lambda/query/*.dist-info/
output.json

lambda/ingest/boto3/
lambda/ingest/botocore/
lambda/ingest/certifi/
lambda/ingest/charset_normalizer/
lambda/ingest/idna/
lambda/ingest/jmespath/
lambda/ingest/opensearchpy/
lambda/ingest/pypdf/
lambda/ingest/requests/
lambda/ingest/requests_aws4auth/
lambda/ingest/s3transfer/
lambda/ingest/urllib3/
lambda/query/boto3/
lambda/query/botocore/
lambda/query/certifi/
lambda/query/charset_normalizer/
lambda/query/idna/
lambda/query/jmespath/
lambda/query/opensearchpy/
lambda/query/requests/
lambda/query/requests_aws4auth/
lambda/query/s3transfer/
lambda/query/urllib3/

lambda/presigned_url/boto3/
lambda/presigned_url/botocore/
lambda/presigned_url/s3transfer/
lambda/presigned_url/jmespath/
lambda/presigned_url/dateutil/
lambda/presigned_url/urllib3/
lambda/presigned_url/six.py
lambda/presigned_url/bin/

lambda/presigned_url/__pycache__/
lambda/presigned_url/*.dist-info/
output6.json
terraform/schema.json

lambda/authorizer/jose/
lambda/authorizer/cryptography/
lambda/authorizer/cffi/
lambda/authorizer/pycparser/
lambda/authorizer/__pycache__/
lambda/authorizer/*.dist-info/
lambda/authorizer/*.egg-info/

lambda/authorizer/ecdsa/
lambda/authorizer/rsa/
lambda/authorizer/pyasn1/
lambda/authorizer/cffi/
lambda/authorizer/cryptography/
lambda/authorizer/bin/
lambda/authorizer/__pycache__/
lambda/authorizer/*.dist-info/
lambda/authorizer/*.egg-info/
lambda/authorizer/six.py
lambda/authorizer/*.pyd
lambda/authorizer/*.so

# Python cache
__pycache__/
*.pyc

# Terraform test artifacts
terraform/payload.json
terraform/response.json
